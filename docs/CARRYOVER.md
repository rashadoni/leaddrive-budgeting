# CARRYOVER — open items awaiting closure

Single source of truth for **open** `🔄` items across substantive turns.

**Closed items are NOT kept here.** Collapsed one-line index →
[`CARRYOVER_ARCHIVE.md`](CARRYOVER_ARCHIVE.md); full resolution narratives →
[`ROADMAP.md`](ROADMAP.md) `## Changelog` + git history.

> **Collapsed 2026-06-28.** The file had grown to 1.2 MB / 1352 lines with 89
> already-✅ rows still parked in `## OPEN` and 232 verbose `CLOSED` rows. Those
> 321 closed rows were collapsed to one-liners and moved to the archive; the 16
> live rows below were compressed to *current state + next action + owner* (the
> historical "how we got here" lives in ROADMAP §Changelog + git).

**Invariants (manual mode — no auto-architect, per [`feedback_no_agents.md`](../.claude/memory/feedback_no_agents.md)):**
- Keep only genuinely-open `🔄`/`📝`/`🚫` rows here. When an item closes, collapse
  it to a one-liner in the archive (or migrate long-term work to `ROADMAP.md §Backlog`).
- `turns-open` (col 3) is optional; bump for stale-row detection. `owner=user`
  rows: re-ping inline in chat when turns-open ≥ 14.
- **Never delete an OPEN row silently** — it either closes (→ archive) or stays
  with a developer-written reason.

## OPEN

| S | date | turns | owner | item |
|---|---|---|---|---|
| 🔄 | 2026-06-28 | 0 | user | **BU-column split SHIPPED + prod-verified for the Actual statements; ONE budget sheet remains (needs domain input).** Cell-scan deploy (`3c057359`) + nginx prod-fix (`d0498d73`: `client_max_body_size` 1M→25M on the `/api/authcheck` auth_request subrequest — was 500-ing ALL `/api/*` POSTs >1MB; bind-mounted single-file conf needs container RECREATE, not just reload, because rsync swaps the inode). **`bu-column-split.ts` (`634dd770`)** splits a consolidated statement into one virtual per-entity sheet per contiguous "BU"-column run (self-labeled → no order-config; PLF/BS/CF; unknown BU value → own skipped block, never merged) + `scanDominantEntity` 2× dominance guard. Dry-run on prod GREEN: PLF/BS Actual 2025 → CPC+holding(AZSF)+EDEN; 2026 → holding+EDEN+CPC+PROMALT — each block on its own company; "EJE" (elimination) block safely skipped; 0 conflicts. **`PLF Budget 2026` is now SAFELY no-op'd (was an EDEN mis-collapse risk).** It has BU_1..BU_4 columns (no plain "BU") that are DIFFERENT dimensions (same row tagged EDEN in BU_1 / CPC in BU_3; BU_1 sub-divides EDEN into Core/Farming over more rows than BU_3 covers) — genuinely ambiguous, can't auto-split. New `hasMultiEntityBuColumn` guard (`634dd770`→ next commit) makes the cell-scan SKIP any multi-entity BU/BU_N sheet → entity null → adapter no-op, so it can NO LONGER mis-write the whole budget onto EDEN on apply. **DOMAIN INPUT still needed to actually IMPORT this budget:** which BU dimension is the owning entity (likely BU_3=col U → EDEN/AZSF/PROMALT/CPC), and whether budget is per-entity or per-entity×sub-unit (Core/Farming). 47 tests; tsc 0; full suite green. [[project_budget_plf_five_blocks]] |
| 🔄 | 2026-06-22 | 0 | user | **Reporting-2026 per-BU budget routing — CODE COMPLETE, awaiting user re-import.** Route now PRE-SPLITS the consolidated `Budget PLF` (5 stacked per-entity blocks) into one virtual sheet per entity (`applyBudgetPlfSplit`; block→entity by verified annual-revenue signature, count-guarded) + `purgeArchivedFirst` on PLF/BS/CF + `import_staging_apply` audit_event. Pushed (`fb4e082e`→`04764ed5`, `0bcb4901`); split byte-proven on the real file (Σ=58,880,103). **User action: the live DATA only refreshes on the next delete→import via the app** (no ANTHROPIC key/tsx in that env). Superseded in spirit by the 2026-06-28 BU-split row above. [[project_budget_plf_five_blocks]] [[project-import-plankind-routing]] |
| 🔄 | 2026-06-21 | 0 | user | **AzerSheker full re-import — P&L DONE, BS/CF deferred.** P&L verified per-entity (CPC 1128 / AZSF 1020 / EDEN 996, plan `Azərşəkər 2026 Actuals`, 3144 lines, orphans 0; Glucose reconciled to the manat; `.R` HO/Region split landed). **BS/CF DEFERRED by design** — single-file flow is P&L-only (`applier.ts:240` 12-month gate); routing to `BalanceSheetLine`/`CashFlowEntry` is spec'd: `docs/superpowers/plans/2026-06-22-bs-cf-single-sheet-routing.md` (do fresh + reviewed; import = #1-risk). **Remaining:** BS/CF (CPC/AZSF/EDEN), Malt P&L, counterparties/court/audit/land. No DB backup (local Postgres). |
| 🔄 | 2026-06-05 | 0 | user | **Manual financial-variable entry (inventory) SHIPPED — needs a real-data live confirm.** `POST /api/budgeting/financial-variable` writes a resolver-conforming `BalanceSheetLine` (manager+, Zod, period-lock, double-count gate, recompute, audit) so `FP_INVENTORY_TURNS` computes; 49 tests; 48 real inventory gaps in DB. **User step:** Indicator Health → expand an inventory gap → enter a REAL period-end figure → confirm it clears live. Deferred (ROADMAP backlog): generalize to receivables/payables/cash. |
| 🔄 | 2026-06-03 | 0 | dev/user | **Residual pre-existing E2E/lint/data debt (NOT audit regressions).** Warm E2E 24/32. DONE: dead onboarding-wizard specs skipped; lint restored (eslint flat config, eslint 9, 161 backlog→warn); **historical 2023-25 P&L/BS/CF loaded** via `scripts/import-historical-financials.ts` (EDEN 2025 = 28% GREEN, exact source match); terminal headline → latest-complete-year (opens on 2025) + partial-year banner; EBITDA subtotal captured all years. **REMAINING:** (i) **PROD DB** — run `import-historical-financials.ts` + `capture-plf-ebitda.ts` against prod `DATABASE_URL` (owner=user, prod creds); (ii) board-deck HERO visual baseline drifted 13% — needs a look before any BASELINE UPDATE; (iii) ATL-DBZ pilot dropdown needs the ATL sub-group seeded. |
| 🔄 | 2026-06-03 | 0 | dev | **Scenario transmission-direction review.** `BRENT_TO_140` (oil spike) flips some grain-cost signals (FP_WHEAT_PRICE_SIGNAL / FP_GRAIN_COST_PRESSURE_BLEND) red→green — an oil shock shouldn't improve grain cost. Wants a dedicated transmission-correctness pass (each driver moves its targets in the economically-sane direction). Not the (fixed) classification bug. |
| 🔄 | 2026-06-02 | 1 | user | **2026 actuals look partial — confirm completeness.** "2026 Actuals" shows only ~9.5M active revenue (exec 13%); 86.9M incl. soft-deleted. Likely only early months / a subset of entities loaded. Data-completeness question, not a code bug. |
| 🔄 | 2026-06-01 | 2 | user | **Client-feedback residuals #1 + #3 (data-blocked).** #1 per-ha benchmark band shipped (floor/target from thresholds) but a TRUE per-crop industry best-case needs a real source; #3 regional-peer comparison needs a regional peer dataset. owner=user (provide figures / dataset). |
| 🔄 | 2026-06-01 | 0 | dev | **Data Sources Catalog — native AZ card prose (P3).** 12 source cards have EN (shipped); AZ falls back to EN. Author native AZ prose for whatItIs/businessValue/interpretation/cadence (12×4) in `sources-catalog-i18n.ts`. Not demo-blocking. |
| 🔄 | 2026-06-01 | 0 | user | **Tier 3 L1 customers — REAL DATA status.** Only MALT/ProMalt real (6 buyers via `import-promalt-customers.ts`; CUSTOMER_HHI 0.50 / TOP 52.4% — genuine red). **MISSING:** Top-10 customers for AZSF/CPC/EDEN + Top-10 suppliers (all entities) — names/AZN/%/terms. Awaiting N. Nəcəfzadə «Müştəri İcmalı» via Azik. |
| 🔄 | 2026-05-27 | 9 | user | **Tier 3 Risk Registry / competitors / NPS — REAL DATA status.** Risk Registry only EDEN (15 KRIs from the real client file); competitiveAdvantage prose only EDEN+CPC; named Top-3 competitors (no schema field) + NPS exist nowhere. owner=user (Azik chase Nəcəf M). |
| 🔄 | 2026-05-26 | 9 | user | **Tier 3 insurance policies — entirely MISSING.** No `Company.settings.insurance` on any entity, no matching OperationalFact. Board Deck compliance section empty. owner=user (Azik chase Əsmər X). |
| 🔄 | 2026-05-30 | 0 | user | **Crisis Brief Phase 1+2+3 — FULLY BUILT + e2e-verified; only an optional LIVE user UX pass remains.** 3-phase living-risk-radar (driver sim no-writes + live-feed anchoring + price/weather + news triggers) shipped & live-verified via the e2e fixture (5 signals → click → cascade ~0.9s warm → brief 61→58). **User step (optional):** login → `/budgeting/terminal` → SCN → run a flagship → confirm cascade pacing / narrative streaming (checklist `docs/superpowers/specs/2026-05-30-crisis-brief-ux-review.md`). |
| 🔄 | 2026-05-05 | 81 | user | **~300+ machine-authored AZ strings need a native-speaker review.** Curated in `docs/AZ_TRANSLATIONS_REVIEW.md` (covers `terminal.*` + 44 live indicator hints, EN/RU/AZ side-by-side + EN-leak flags). The `admin*` namespaces (~265 newer strings) are NOT yet in the doc — regenerate when the review is finally scheduled. Blocker = a native AZ speaker (the only remaining step). |
| 🚫 | 2026-05-08 | 21 | developer | **Tier 2 H1 import-excel handler tests — DEFERRED.** Auth/rate/size/period-lock already covered (5 cases). The rest (xlsx parse correctness + AI-mapper pipeline + multi-table writes + idempotency) needs 3-4 hand-authored fixture .xlsx + golden-sum assertions (~6-8h). **Re-trigger:** a prod ingestion bug, OR a dedicated fixture-authoring session. |
